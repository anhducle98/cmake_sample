CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES (lib)

ADD_LIBRARY (helper STATIC lib/b.cpp)
ADD_EXECUTABLE (main a.cpp)
TARGET_LINK_LIBRARIES (main helper)

INSTALL (TARGETS main DESTINATION bin)
